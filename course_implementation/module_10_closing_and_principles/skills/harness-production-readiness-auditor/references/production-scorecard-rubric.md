# Production Harness Scorecard Rubric

1. **Gate 1: Memory Files (20%)**: `CLAUDE.md` and `AGENTS.md` present.
2. **Gate 2: PreToolUse Hooks (20%)**: Registered in `.claude/settings.json`.
3. **Gate 3: Test Runner (20%)**: Automated runner with live pytest exit 0.
4. **Gate 4: MCP Tools (20%)**: Valid `@mcp.tool` and `@mcp.resource` declarations.
5. **Gate 5: Subagents (20%)**: Valid agent frontmatter definitions.
