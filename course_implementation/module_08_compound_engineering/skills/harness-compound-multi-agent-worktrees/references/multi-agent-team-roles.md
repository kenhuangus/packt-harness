# Multi-Agent Specialized Roles

1. **Planner**: Read-only agent with large context, creates structured breakdown.
2. **Implementer**: Sandboxed inside an ephemeral Git worktree with focused sub-spec.
3. **Reviewer**: Fresh context window agent that runs independent AST and pytest checks.
